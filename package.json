{
  "name": "superagent-get-chunked",
  "version": "1.0.0-alpha.0",
  "description": "Adds support to the superagent library for chunked-transfer encoded responses when in the browser.",
  "main": "index.js",
  "scripts": {
    "build": "npm run compile && npm run compress",
    "compile": "npm-run-all compile:*",
    "compile:main": "browserify index.js -o ./test/browser/superagent-get-chunked.js && echo 'onmessage = function(e) {' > ./test/browser/superagent-get-chunked-worker.js && browserify ./superagent-worker-body.js >> ./test/browser/superagent-get-chunked-worker.js && echo '};' >> ./test/browser/superagent-get-chunked-worker.js && cp ./test/browser/superagent-get-chunked-worker.js ./test/browser/superagent-get-chunked-worker.min.js",
    "compile:csv": "browserify ./csv.js --standalone csvparser -o ./test/browser/csv.js",
    "compile:ndjson": "browserify ./ndjson.js --standalone ndjsonparser -o ./test/browser/ndjson.js",
    "compile:n3": "browserify ./n3.js --standalone n3parser -o ./test/browser/n3.js",
    "compile:tsv": "browserify ./tsv.js --standalone tsvparser -o ./test/browser/tsv.js",
    "compile:xml": "browserify ./xml.js --standalone xmlparser -o ./test/browser/xml.js",
    "compress": "uglifyjs ./test/browser/superagent-get-chunked-worker.js -c --debug -o ./test/browser/superagent-get-chunked-worker.min.js",
    "installCustomChunkedRequest": "cd ./node_modules && rm -rf ./chunked-request && git clone https://github.com/ariutta/chunked-request.git && cd ./chunked-request && npm install && npm run prepublish && cd ../..",
    "postinstall": "npm run installCustomChunkedRequest",
    "start": "echo 'Once build finishes, visit http://localhost:8080' && npm run build && cd ./test/browser && http-server",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "git+ssh://git@github.com/ariutta/superagent-get-chunked.git"
  },
  "keywords": [
    "request",
    "superagent"
  ],
  "author": "Anders Riutta",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/ariutta/superagent-get-chunked/issues"
  },
  "homepage": "https://github.com/ariutta/superagent-get-chunked#readme",
  "dependencies": {
    "chunked-request": "ariutta/chunked-request#30bd49c6c2435255697240f9028438a5b5808cf0",
    "component-emitter": "^1.2.1",
    "install": "^0.8.1",
    "js-csvparser": "^1.4.0",
    "lodash": "^4.13.1",
    "n3": "^0.5.0",
    "npm": "^3.10.5",
    "npm-run-all": "^2.3.0",
    "sax": "^1.2.1",
    "superagent": "^2.1.0"
  },
  "devDependencies": {
    "browserify": "^13.0.1",
    "component-emitter": "^1.2.1",
    "http-server": "^0.9.0",
    "uglify-js": "^2.7.0",
    "uglifyify": "^3.0.2"
  }
}
